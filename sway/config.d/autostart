#
### Autostart
#
# Give waybar some time to start so systray is
# available when autostarting apps

exec keepassxc-proxy 
exec sh -c "sleep 2" && keepassxc
exec sh -c "sleep 2" && nextcloud
exec sh -c "sleep 2" && QT_SCALE_FACTOR=1 QT_QPA_PLATFORM=xcb megasync >/dev/null
